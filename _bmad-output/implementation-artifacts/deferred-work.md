- source_spec: spec-2-6-alertas-badge-geracao-on-demand.md
  summary: Implementar alerta de dividendo esperado não recebido quando o Épico 3 persistir a tabela dividends.
  evidence: A Story 2.6 não possui fonte persistida de dividendos e a geração por transações de provento criaria falsos positivos.
# Trabalho diferido

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-adicionar-posicao-manual.md`
  summary: Criar a tabela `transactions`, o enum `transaction_type` e o recálculo server-side de `positions` (`recalculate_position()` + triggers).
  evidence: Separado da Story 2.2 no gate de orçamento de tokens do spec (~2.500–3.200 contra teto de 1.600). Nenhuma tela da 2.2 cria transação — a primeira fonte é a importação CSV da Story 2.5 — e o AC da 2.2 exige que a posição valha sem transação alguma (AD-8), então o bloco não tem superfície de usuário nesta story. Decisões já tomadas, a preservar quando este trabalho for retomado: (1) preço médio é média móvel ponderada com baixa a custo médio, calculada percorrendo transações por `transaction_date` — a fórmula agregada de `stories-detailed-DEPRECATED.md` L503–546 é insensível à ordem e devolve 16,67 onde a média móvel devolve 20,00 na sequência compra 100@10 → venda 50@20 → compra 50@30; (2) quantidade negativa é proibida, e venda que zera a posição a apaga, o que mantém `CHECK (quantity > 0)` coerente; (3) o trigger dispara em INSERT, UPDATE e DELETE, retorna `COALESCE(NEW, OLD)` e não usa `SECURITY DEFINER`; (4) `positions.acquisition_date` é `NOT NULL` e no caminho do trigger precisa ser derivada de `MIN(transaction_date)` das compras, senão o upsert viola a constraint — esse conflito volta a valer quando o trigger existir. Schema de referência: `ARCHITECTURE-SPINE.md` L369–413, com nomes de índice no padrão do repo (`<tabela>_<cols>_idx`), não `idx_*`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-visualizar-lista-de-posicoes.md`
  summary: Flag "Cotação antiga" considera apenas fim de semana; feriados da B3 (Carnaval, Corpus Christi, feriados da praça de SP) ainda produzem falso-positivo até existir calendário de pregões.
  evidence: Story 2.3 renegociou o AC para "> 1 dia útil" (opção A1), que pula sábado/domingo com aritmética simples mas não conhece feriados B3. A alternativa robusta (A2) é comparar cada `date` com o pregão mais recente presente no próprio `price_history` — não precisa de calendário externo nem de "hoje". Retomar quando o Épico 3 trouxer o calendário de proventos/pregões, ou implementar A2 sobre `price_history`.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-composicao-por-classe-e-exposicao-internacional.md`
  summary: A leitura de `positions` com join do catálogo em `positionService` usa `as unknown as PositionWithAsset[]`, então `tsc` não garante que `type` (e demais colunas) sigam em `ASSET_COLUMNS`; remover `type` do select compilaria e só quebraria em runtime (composição vira uma fatia "Outros" de 100%).
  evidence: Gap de type-safety pré-existente (o cast antecede a Story 2.4), exposto incidentalmente porque a 2.4 passou a depender de `type` vir do select. Correção fora do escopo desta story: tipar o retorno do PostgREST ou cobrir o select com um teste que valide as colunas pedidas. Sem branch de UI própria — é robustez de contrato de dados.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-transactions-schema-e-recalculo.md`
  summary: Importação CSV de transações — parser (ISO-8859-1/BOM, separador `;` com decimal `,`), validação com erro por linha/coluna/problema + exemplo, fluxo de UI de 2 etapas (upload → preview com correção inline → confirmar), limite 1000 linhas / preview de 500 em <2s, e a gravação em `transactions` que dispara o recálculo.
  evidence: Separado da Story 2.5 no gate de escopo (decisão do usuário, opção B). A 2.5 é um objetivo único mas grande demais para uma spec; a costura natural é backend (esta story: schema `transactions` + `recalculate_position()` + triggers + RLS, testável isoladamente por testes RLS/SQL) vs. CSV+UI (esta entrada). O CSV+UI depende do backend já mergeado e provado. AC da 2.5 a preservar aqui: colunas `data, ticker, tipo, quantidade, preço, corretagem`; tipos `buy|sell|dividend|jcp|bonus`; ticker desconhecido marca a linha (corrige ou pula); após confirmar, cada ticker importado dispara o recálculo server-side (que já existirá). Referência de UI: fluxo de 2 etapas do epic-2-context; padrão de `Modal`/service+hook das stories 2.2–2.4.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-transactions-schema-e-recalculo.md`
  summary: `recalculate_position()` não serializa gravações concorrentes ao mesmo `(user_id, ticker)`: sob READ COMMITTED, dois writes simultâneos podem se basear em snapshots defasados e um "last-writer-wins" perder um evento. Um `pg_advisory_xact_lock` por par resolveria.
  evidence: Fora do cenário do MVP (um usuário importando o próprio CSV serialmente). Real sob concorrência multi-escritor no mesmo par, que nenhuma tela produz hoje. Retomar se surgir escrita concorrente (ex.: sync automático + edição manual simultâneos).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-transactions-schema-e-recalculo.md`
  summary: O enum `transaction_type` não cobre desdobramento/grupamento (split) nem amortização de FII; esses eventos societários causariam drift permanente entre `transactions` e `positions` quando existirem. Também: uma posição criada manualmente (2.2) é sobrescrita/apagada assim que existe qualquer transação para o mesmo ticker (o `ELSE DELETE`/UPSERT trata `transactions` como fonte de verdade daquele par).
  evidence: Enum fixado pela arquitetura (`ARCHITECTURE-SPINE.md` L392) com 5 valores; novos tipos são de épico futuro. A colisão manual-vs-importado é decisão de produto da UI de importação (próxima story) — o preview/confirmação é onde avisar o usuário. Nenhuma superfície nesta fatia de banco.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-tela-patrimonio-evolucao-composicao-e-exposicao.md`
  summary: wealthService.listPriceHistory usa limit = tickers.length × 365; se o PostgREST tiver cap menor (padrão 1000), a série é truncada silenciosamente para carteiras com >2 tickers e período '1A'/'Tudo'.
  evidence: Com 50 tickers (limite do MVP) × 365 = 18.250 linhas, a query ultrapassa o padrão de 1000 linhas sem `Range` header. O Supabase permite aumentar o cap ou usar paginação; não foi corrigido nesta story porque: (a) o seed tem poucos ativos e o impacto no MVP é mínimo, (b) a correção adequada (paginação ou Supabase `Range`) ultrapassa o escopo de uma story de tela, e (c) buildWealthSeries usa abordagem permissiva que mascara truncamentos como gaps parciais em vez de erros óbvios. Retomar antes de aumentar o catálogo de ativos.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-criar-e-editar-regras-de-score.md`
  summary: Adicionar testes de integração para hooks e ScoreRuleForm (create/update/delete/upsert e coerção threshold_max→null).
  evidence: Nenhum teste cobre os caminhos de escrita dos hooks nem o comportamento de onSubmit do ScoreRuleForm; padrão de mock de Supabase já existe no projeto (supabaseMock.ts). Não causa bug imediato pois build+lint+testes de validação passam, mas regressões em payloads ou cache invalidation não seriam detectadas automaticamente.
